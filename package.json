{
  "name": "medprism",
  "private": true,
  "version": "2.0.6",
  "description": "Medical scientific writing workspace",
  "author": "MedHorizon",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "auth:server": "node server/auth/index.mjs",
    "compile:server": "node server/compile/index.mjs",
    "electron:dev": "electron .",
    "fetch-tectonic": "node scripts/fetch-tectonic.mjs",
    "electron:pack": "node scripts/fetch-tectonic.mjs && npm run build && electron-builder --publish never && node scripts/fetch-tectonic.mjs --assert-unpacked",
    "electron:pack:win": "node scripts/fetch-tectonic.mjs win32-x64 && npm run build && electron-builder --win --x64 --publish never && node scripts/fetch-tectonic.mjs --assert-unpacked win32-x64",
    "electron:pack:linux": "node scripts/fetch-tectonic.mjs linux-x64 && npm run build && electron-builder --linux --x64 --publish never && node scripts/fetch-tectonic.mjs --assert-unpacked linux-x64",
    "electron:pack:mac": "node scripts/fetch-tectonic.mjs darwin-arm64 && npm run build && electron-builder --mac --arm64 --publish never && node scripts/fetch-tectonic.mjs --assert-unpacked darwin-arm64",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "pdfjs-dist": "^5.4.149",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router-dom": "^7.18.2"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "electron": "37.10.3",
    "electron-builder": "^26.0.12",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^3.2.4"
  },
  "build": {
    "appId": "icu.medhorizon.medprism",
    "productName": "MedPrism",
    "copyright": "Copyright © MedHorizon",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json",
      "!electron/**/*.test.*",
      "shared/**/*"
    ],
    "asar": true,
    "publish": {
      "provider": "generic",
      "url": "https://ghfast.top/https://github.com/medhorizon/medprism/releases/latest/download"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "MedPrism-${version}-win64.${ext}"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "artifactName": "MedPrism-${version}-win64-setup.${ext}"
    },
    "linux": {
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64"
          ]
        }
      ],
      "category": "Office",
      "artifactName": "MedPrism-${version}-linux64.${ext}"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "arm64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "arm64"
          ]
        }
      ],
      "category": "public.app-category.productivity",
      "artifactName": "MedPrism-${version}-macos-arm64.${ext}",
      "identity": null
    },
    "dmg": {
      "artifactName": "MedPrism-${version}-macos-arm64.${ext}"
    },
    "extraResources": [
      {
        "from": "resources/tectonic",
        "to": "tectonic",
        "filter": [
          "**/*"
        ]
      }
    ]
  }
}
